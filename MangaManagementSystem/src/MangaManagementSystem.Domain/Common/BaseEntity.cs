using System;

namespace MangaManagementSystem.Domain.Common
{
    public abstract class BaseEntity
    {
        // Only include audit fields if present in the table
        // Id type will be set in each entity (int, long, short)
    }
}
